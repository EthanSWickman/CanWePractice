understand current repository

understand secrets file structure and rebuild it

make a clean list of commands/functionality that will be required

create necessary data structures

create API calls that are parsed into data structures and returned to the calling commands

create config file that contains settings to be adjusted for administrators

create administrative route so that settings can be changed via the Discord interface
